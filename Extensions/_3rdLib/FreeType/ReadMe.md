# ReadMe

download [FreeType](https://freetype.org/download.html) and build it according to the document.

FreeType already provides MSBuild support, simply use the sln to build as static lib, and put results under lib folder, Win32 & x64 respectively.
